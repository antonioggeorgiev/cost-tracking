enum SpaceRole {
  owner
  editor
  viewer

  @@map("WorkspaceRole")
}

enum ExpenseStatus {
  planned
  pending
  posted
  cancelled
}

enum ExpenseType {
  one_time
  recurring_generated
  debt_payment
  manual_adjustment
}

enum InviteStatus {
  pending
  accepted
  revoked
  expired
}

enum RecurringFrequency {
  weekly
  monthly
  yearly
}

enum RecurringTemplateKind {
  fixed_amount
  variable_amount
}

enum DebtAccountKind {
  bank_loan
  personal_loan
  leasing
}

enum DebtDirection {
  i_owe
  they_owe_me
}
